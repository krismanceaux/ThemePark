@model IEnumerable<ThemePark.PERMIT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ride.RideName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ticket.TicketNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PTimeStamp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ride.RideName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ticket.TicketNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PTimeStamp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
