@model IEnumerable<ThemePark.SPHLogin>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutS.cshtml";

}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SeasonPassHolder.StreetAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pswd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SeasonPassHolder.StreetAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pswd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LoginEmail }) |
            @Html.ActionLink("Details", "Details", new { id=item.LoginEmail }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LoginEmail })
        </td>
    </tr>
}

</table>
